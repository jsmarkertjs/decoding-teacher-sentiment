# Core
tensorflow>=2.12.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.2.0

# Text processing
regex>=2023.0.0       # Used in clean_for_nn (via re standard library — included for docs)

# Explainability
lime>=0.2.0

# Scraping (optional — only needed if running the scraper)
curl_cffi>=0.5.0

# Visualization
matplotlib>=3.7.0

# Notebook
jupyter>=1.0.0
ipython>=8.0.0

# Demo
streamlit>=1.28.0